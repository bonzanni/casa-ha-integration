{
  "config": {
    "step": {
      "user": {
        "title": "Casa",
        "description": "Connect to your Casa add-on.",
        "data": {
          "host": "Host",
          "port": "Port",
          "webhook_secret": "Webhook secret"
        }
      },
      "hassio_confirm": {
        "title": "Casa add-on discovered",
        "description": "Connect to {name}?"
      },
      "reauth_confirm": {
        "title": "Reauthenticate Casa",
        "data": {
          "webhook_secret": "Webhook secret"
        }
      }
    },
    "error": {
      "invalid_auth": "Invalid webhook secret.",
      "invalid_catalog": "Casa returned an invalid voice-agent catalog. Check Casa's enabled ha_voice agent names and configuration.",
      "cannot_connect": "Cannot reach the Casa add-on.",
      "unknown": "Unexpected error."
    },
    "abort": {
      "already_configured": "Casa is already configured",
      "reauth_successful": "Re-authentication successful"
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "satellite_entity_overrides": "Satellite entity overrides (JSON)"
        },
        "data_description": {
          "satellite_entity_overrides": "Usually leave this as {}. Configure it only when one Home Assistant device has multiple assist_satellite entities and Casa cannot determine where to speak a queued result. Example: {\"abc123\":\"assist_satellite.kitchen_voice\"} makes results from device abc123 play on the kitchen voice satellite. Without an override, Casa safely declines an ambiguous announcement rather than speaking on the wrong device."
        }
      }
    },
    "error": {
      "invalid_satellite_entity_overrides": "Enter a JSON object whose assist_satellite entities are currently bound to the matching device IDs."
    }
  },
  "config_subentries": {
    "voice_agent": {
      "title": "Casa voice agent",
      "step": {
        "reconfigure": {
          "title": "Configure voice agent",
          "data": {
            "session_mode": "Session scope",
            "transport": "Transport",
            "idle_stability_ms": "Assist idle stability (milliseconds)"
          },
          "data_description": {
            "session_mode": "How memory scope is derived: per device, per user, or per conversation.",
            "transport": "Prefer WebSocket for voice session registration and background results, or use SSE as a stateless fallback.",
            "idle_stability_ms": "How long Casa waits after the voice assistant becomes idle before speaking a queued specialist result. This prevents Gary from interrupting an active conversation or follow-up. If the device is already idle, the result is spoken immediately. Default: 750 ms."
          }
        }
      },
      "error": {
        "invalid_idle_stability": "Enter a whole number from 0 to 5000."
      },
      "abort": {
        "catalog_managed": "Voice agents are discovered from Casa and cannot be added manually.",
        "reconfigure_successful": "Voice-agent settings updated."
      }
    }
  }
}
